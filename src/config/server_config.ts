export const enum Listen {
    PORT = 8000,
    SOCKET_PORT = 8888
}

export const enum Status {
    SUCCESS = 200,
    USE_LAST_RESOURCE = 304,
    REJECT_REQUEST = 403,
    SERVER_NOT_FOUNT = 404,
    SERVER_ERROR = 500
}
